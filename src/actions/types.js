// We do not use export default because we expert here to have many different actions
// because of that we import email change like this:
// import { EMAIL_CHANGED }

export const EMAIL_CHANGED = 'email_changed';
export const PASSWORD_CHANGED = 'password_changed';
